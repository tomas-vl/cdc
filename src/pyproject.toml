[project]
name = "project"
version = "0.1.0"
requires-python = ">=3.14.0"
dependencies = [
  "defopt>=7.0.0",
  "numpy>=2.4.6",
  "tdqm>=0.0.1",
  "torch>=2.11.0",
  "torchvision>=0.26.0",
  "transformers>=5.9.0",
]

[tool.uv.sources]
torch = [
    { index = "pytorch-cpu" },
]
torchvision = [
    { index = "pytorch-cpu" },
]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true
